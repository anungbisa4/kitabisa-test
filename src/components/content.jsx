import React, { Component } from 'react'

export default class Content extends Component {
  render() {
    return (
      <div>
        {/* { this.props.title }
        <div>{ this.props.content.header }</div>
        <div>{ this.props.content.body }</div>
        <div>{ this.props.content.footer }</div> */}
      </div>
    )
  }
}
